{% extends 'header/header.html.twig' %}

{% block body %}

    {% for item in navigation %}
        <a href="{{ item.url }}" class="nav__link js-menu-link">{{ item.name }}</a>
    {% endfor %}

{% endblock %}
